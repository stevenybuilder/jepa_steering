# Refined edit: remaining task-specific table cells — September 11, 2026

This is completion of the eight already requested cells on Push-T, PointMaze,
Wall and DROID. It is not a new intervention search, confirmation population,
training history, HMM or combined successor. Preserve every older result and
freeze. Do not transfer a MetaWorld fitted bank to another task.

## Fixed recipe and fitting population

Push-T/PointMaze/Wall use their existing task-specific BF16 operator-rank fits:
the original 128 eligible fitting families, B3/H3, 256 patches, 400 features,
native H6 target residual, and the source task-specific delivered-L2 dose.
Only the first four learned and random basis directions enter the refined edit.
Use the identical `fixed_response_rank4_v1` arithmetic already tested on
MetaWorld: an intercept and three native PCA scores, a fitted4×4 response map,
one-pass native-feature extraction, and no online finite differences or shadow.

Calibrate mean responses on24 fitting families and audit on8 other fitting
families, four author-native clip/prefix examples each, selected by the unchanged
seed2026090802 hash ordering. Use16 fitting-only signed probes per example,
four probes batched together, BF16 native forecasts with FP32 map arithmetic.
Invert the family-balanced mean response Gram once, with the unchanged damping
rule0.01×mean diagonal. Do not average per-example inverse maps. The audit is
held out from response calibration, not from the inherited128-family PCA fit;
it is not fresh confirmation. No validation outcome determines admission/dose.

DROID needs a distinct12-block/H3 adapter and its own fitted directions/readout
on the previously audited128 non-evaluation recordings and512 native prefixes.
Its task-specific fitting and behavioral executable freeze must be completed
before launch. Its architecture is not assumed equivalent to the three
six-block models, and no DROID bank is manufactured by reshaping another task.

### DROID architecture mapping, fixed before its refined fitting outcomes

Use the output of zero-indexed predictor block6 at model stepH3: the midpoint
block of its12-block predictor, matching the midpoint convention B3/6blocks,
not the coupling intervention's predictor-input site. Read all256 newest-frame
patches with1024features and predict the native H3 visual residual. This is our
explicit task adaptation, not a steering method proposed by the JEPA-WM authors.
Use the original audited128 recordings ×4 prefixes, native4-frame loader and
three recorded model actions; verify exact pixels/actions/state/prefix identities
after any path relocation. Preserve no-proprio inputs, context capacity2, BF16
fitting and strictFP32 behavioral execution. Refit only this task's PCA/readout
and response maps: no additional layer/spatial search or online solver.

Fit nested eight-axis activation PCA, use its first four directions and a
separately seeded orthonormal Gaussian basis (original support seed2026090721).
Fit an intercept-plus-three-score OLS readout on the same512 prefixes. Dose and
signed response radius are0.005 times the centered native field's RMS L2 norm,
the same fit-only rule as the simulation refined edit. Hash-order recording
identities with seed2026090802 for24 calibration and8 response-audit recordings.
Keep the existing four-probe batches, family-balanced response average and
0.01-trace-scaled damped4×4 solve. The audit is not independent confirmation.

### Implementation status during September11 continuation

The six-block refined fitting and behavioral adapters have CPU contract tests;
GPU fitting, receiving checks and scientific panels are not yet complete.
Push-T's original bank is verified. The checked navigation archive contains
receipts but not the required rank-bank tensor files; this is a recovery gap,
not evidence that their historical outcomes are invalid. Do not represent a
new fit as recovery of the byte-identical historical bank.

September11 recovery amendment: if the original navigation rank tensors cannot
be located in the preserved archives, reconstruct **only** the native B3 PCA
bases and three-score H6 residual readout on the same128 families/512 prefixes.
Use the existing pinned BF16 model/loader, sample-space PCA8, fixed-sign convention,
and Gaussian control seed2026090721. Retain the archived task-specific dose and
response radius; report the newly calculated fit-only dose as a diagnostic, never
select it from validation results. This avoids repeating the old geometry,
layer and spatial fitting suite. Label the output a **new limited reconstruction**,
with its own source/input hashes and the original missing bank's expected hash;
do not claim it is the recovered original tensor file. The subsequent response
fit and behavioral comparisons retain the same registered recipe and population.

## Behavioral design, fixed before new outcomes

For each task, compare native, refined four-direction edit, and the separately
response-calibrated random-subspace edit. Keep the original released checkpoint,
official planner, full horizon/iteration/candidate/action budget, source stimulus
sampler, precision and persistent RNG streams. Preserve96 TOTAL evaluations per
simulation condition and64 TOTAL recorded-plan evaluations per DROID condition.
The existing populations have been exposed: this is development/replication,
not independent confirmation. No protected base-1 access is granted.

Collect receiving-runtime paired native references; verify their exact input
and outcome compatibility with historical references before any combined table.
Different native reproduction scores must be disclosed, not silently substituted.
Randomize no robot action: the random arm changes only its internal edit basis.
Each scenario's three conditions remain on the same physical GPU. Split whole
original RNG streams, never pad samples or splice fresh RNG into partial streams.

The fixed primary contrast family has12 contrasts: each of the two edits versus
native and refined versus its random control, on each of four tasks. Report the
official task success for simulation and the native DROID recorded-action score
separately. Use20,000 paired cluster bootstrap draws, seed2026091102, Bonferroni
simultaneous95% intervals across12 contrasts. Cluster Push-T by original source
initial-state family, navigation by exact initial/goal pair, and DROID by source
recording. Report all effects, including harms and nulls; no partial-result
selection, automatic confirmation or pooled robotics-success score.

## Verification and execution

Each new GPU/task must pass full native/repeat/zero-dose/refined/random excluded
engineering episodes, exact paired inputs, specified edit energy, unchanged
checkpoint and one backend call per forecast. Compare the refined arithmetic
with an independently constructed static-field reference at native candidate
counts. Source/fit/data/planning/freeze hashes and raw action/model-call traces
are mandatory. Partial output is retained, not counted as a finished cell.

Reuse verified historical fitting banks and source inputs; do not rerun the full
old rank/spatial/layer fitting suite just to reconstruct a missing file. Direct
worker downloads, small sample-space PCA and a precomputed response inverse
reduce transfer, memory and repeated work. These are not claims of measured
end-to-end speedup. US-only aggregate$7/hour cap and full Drive preservation
before routine worker stop apply.
