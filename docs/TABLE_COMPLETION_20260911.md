# Released-checkpoint table completion

## Latest:scheduled collections and analyses complete

Wall/PointMaze's full1,728-record,nine-arm-per-task,32-contrast analysis is now
complete,alongside Push-T's864-record analysis. All results-supported cells are
in [the consolidated table](../reports/SIX_TASK_RESULTS_20260911.md). Fourteen
cells remain unrun; they are not gaps that analysis can fill. All GPU instances
were verified stopped after the final raw-result Drive snapshot. The analysis
used the local CPU and unchanged preserved scientific source,no new inference.

## September11,17:34UTC current checkpoint

Push-T's complete nine-arm864-record frozen analysis now passes input, source,
receiving-device and pairing checks. Equal-budget coupling61.46% versus native
59.38%; its random control60.42%. All16 source-family-clustered simultaneous
intervals include or touch zero. Five displayed blanks filled; fourteen remain
unlaunched because they require task-specific implementation/engineering.
See [current table](../reports/SIX_TASK_TABLE_PROGRESS_20260911.md).

Four GPUs on the one active Texas worker continue PointMaze's original132
missing control episodes. A timeout-margin fallback waits for original queues
to terminate, skips every verified complete stream, and uses only an empty
device with its original receiving proof. No altered scientific protocol,
new rental, deadline extension or inference-precision change. The completion
collector includes both fallback queues and the final Push-T audit in its
Drive-preservation-before-stop barrier; independent budget guard remains live.

## September11,16:54UTC current checkpoint

All696 missing Push-T episodes are collected and their result snapshots have
verified Drive receipts; final full-panel coverage/pairing analysis is next.
The two finished two-GPU workers stopped. Texas50588893 now assigns all four
GPUs to PointMaze receiving checks, then the132 missing original control
episodes. The isolated legacy runtime reproduces97 original initial/goal input
pairs exactly. No completed result was invalidated by the failed dependency-only
startup, and no new frozen arm/sample/precision/planner choice was substituted.

Literature and control-definition audit:
[matched random control](../matched%20random%20control.md), including a full LEAP
PDF follow-up distinguishing random-action policies from active random edits.
The orientation-only supplemental control is a recommendation, not a launched
replacement. Six missing MetaWorld component cells and eight refined/control
non-MetaWorld cells still need task-specific engineering and remain unlaunched.

## September11,12:25UTC expansion checkpoint

The latest Wall continuation completed all120 assigned episodes;204 raw/log/source
members and the complete compressed archive were independently checked locally
and by full Google Drive byte readback (`1ZZxH-pBNc7Iwki7T863VqIZ2Ym8ZSA6q`).
The old collector failed on missing rclone PATH; the12:00UTC budget stop worked.
The backup was completed from the intact local archive without a GPU restart.

Recovering exact archives fills ten displayed cells: five coupling/component
arms on each of Wall and PointMaze. Their96-episode descriptive success scores
do not establish significance before the full frozen32-contrast navigation
analysis. Nineteen displayed cells remain: five Push-T coupling/component cells,
six MetaWorld component cells, and eight refined-edit/control cells on the four
non-MetaWorld tasks. The latter require task-specific engineering/fits/freezes;
they are not already running or inherited from a different model architecture.

The user renewed7USD/hour and added account credit (live26.849986USD). Prepared
stopped workers could not reacquire GPUs and their restart requests were cancelled.
New owned leases: Indiana50588615 four GPUs for PointMaze11 missing streams;
Texas50588893 four GPUs and California50588903/50588914 two GPUs each for disjoint
Push-T slices of58 streams. Combined selected-disk running quote6.223333USD/hour,
older retained-storage charges separate within the cap. Four workers are in
image startup/staging; launch/receiving/output receipts, not leases, establish
actual experimental progress. Paused confirmation/history/HMM work stays paused.
Explicit supervisors advance each GPU only after its receiving checks. Periodic
raw snapshots go to verified Drive; finished workers stop while retaining disks.
Independent guard stops on six-hour deadline or credit below1USD; no top-up purchase.

Original total counts, planner settings, precision, fitting populations and control
seeds remain unchanged. Hash-bound stream reconstruction across archive batches
prevents a false rerun of PointMaze joint/shard7. See the September11 section in
`jax_scaling_notes.md` for implemented scheduling/I/O changes and limits.

User authorization: September 11, 2026; fill the missing intervention/task cells
in the six-task table. This is not a new confirmation or retraining campaign.

## Fixed scope

- Tasks: Reach, Reach-Wall, Push-T, PointMaze, Wall, DROID.
- Methods: inexpensive fixed-response four-direction edit and its random
  control; static visual, action-conditioning, joint, equal-budget joint and
  their existing controls. Retain the previously frozen permuted controls
  needed to finish registered coupling analyses even when not shown in the
  abbreviated user table.
- Counts: 96 total simulator episodes per task/condition; 64 recorded-plan
  endpoints per DROID condition. GPU count never multiplies the sample size.
- DROID action score remains distinct from simulator task success.
- Preserve old scientific freezes, fits, exclusions, failed runs and partial
  episodes. No native-success field from DROID's dummy environment is used.

## Work order

1. Restore checksum-bound native references, complete candidate shards, frozen
   source snapshots and fits from their grounded Drive archives. Reconcile
   duplicates by exact hashes and episode IDs, not by choosing outcomes.
2. Verify and analyze complete existing panels; distinguish analysis-only gaps
   from genuinely missing evaluations. No paid rerun of verified complete cells.
3. Restore the original environments/checkpoints on owned US workers, validate
   them, then finish missing intact persistent RNG streams. Interrupted streams
   are replayed separately from their beginning unless exact saved RNG state is
   supported; old partial records stay preserved and are not double-counted.
4. Complete task-specific engineering for genuinely new refined-edit and
   MetaWorld factorial cells, freezing exact recipe mappings, fit provenance,
   arm registry and simultaneous contrasts before their outcomes. A ready
   MetaWorld method is not automatically a ready DROID method.
5. Reload raw outputs, verify pairing/completeness, run the frozen analyses and
   update the six-task table with explicit score types and uncertainty.
6. Preserve new raw results, traces, logs, source and receipts in private Google
   Drive with verified readback; stop finished owned GPUs. No disk destruction
   without established preservation and applicable authorization.

## Compute and storage

Maximum aggregate running fleet rate: $7/hour under the latest user budget.
Original50546172 could not reacquire GPUs; its queued start was cancelled.
Replacement50561030 is Washington, two RTX4090s and100GB at$0.848889/hour.
Its initial$4 compute/estimated-existing-storage envelope has an independent
retained-disk stop deadline of2026-09-11T10:37:01.834Z. This is not a promise
that all expanded table cells fit the account's remaining$7.22 shown credit.
Update09:36UTC: live credit read$9.317704; the ten-stream Wall continuation
uses the same rental, with a superseding12:00UTC hard stop and immediate
verified-preservation/stop on terminal queue status. No additional rental.
Each GPU has one independent workload, separate output roots and no sample
padding. Other workers activate only for ready independent work, within the cap.
The laptop had approximately3.8GiB free at preflight: keep large datasets and
weights on worker storage and use bounded local result recovery.

The earlier confirmation panel, HMM extension, combined successor, training
histories, protected populations and legacy expensive rank solver remain paused.
Current authorizations are recorded separately from historical config fields.
Preparation or a launched process is not a completed scientific comparison.

## September 11 control-audit checkpoint

The full original core archive was restored on worker storage:6,872 files,
all Drive-part/archive/member hashes verified. A new CPU-only supplemental
audit checked all960 episodes, raw traces, paired initial/goal stimuli and seed
assignments, actual edit doses, and exact reconstruction of the coupling random
directions from frozen seeds. No setup/dose mismatch was found in these checks;
random-direction robustness remains untested. The original primary analysis is
unchanged. See [control audit](../reports/MATCHED_RANDOM_CONTROL_AUDIT_20260911.md).

Five additional small navigation/Push-T archives are restored and hash-verified.
No new scientific episodes were collected during the audit. GPU runtime setup
hit CUDA-wheel download timeouts; a fresh Python3.11 environment now inherits the
image's exact Torch2.7.1+cu128 stack instead. This Python-minor difference still
requires exact receiving-native checks before resuming any frozen simulation
streams. No equivalence is assumed from matching package labels alone.

## Wall scientific continuation, 09:36 UTC

All11 receiving-engineering episodes passed, including exact historical native
and zero-dose action/outcome reproduction. They completed07:41UTC, but the
scientific queue was not connected; both GPUs were found idle at09:28UTC. This
was an orchestration failure, not a scientific dependency or GPU scarcity.

The new explicit queue verifies54 complete old streams, rejects conflicting
duplicates and preserves all old outputs. Only ten absent streams are launched:
permuted_visual1–7 and permuted_joint0–2, twelve episodes each,120 total. Each
GPU gets five intact streams; no scenario padding, refitting or new direction
draws. The exact archived module runs the original frozen scientific protocol.

Actual scientific children4890/4891 were observed running at100% utilization on
GPUs0/1. One local collector polls completion/failure independently of this chat,
archives raw new episodes/logs/proofs/source to Drive with byte readback, and
stops the rental while retaining its disk. A separate12:00UTC provider stop
bounds spending if collection fails. Other missing task panels are not running.
