# Fixed combined-development stage — 2026-09-07

This implements step 5 of [EXPERIMENT_PLAN.md](EXPERIMENT_PLAN.md). It is an
intervention-study extension, not an experiment performed by the JEPA-WM authors.
The corrected author-loader/context/loss protocol and complete released primary
validation pools remain the basis of development. No fresh confirmation is opened.

## Frozen scope

Reach has eligible equal-budget coupling and rank-4 components. The plan assigns
unscaled coupling to factorial interaction estimation, equal-budget coupling to
efficacy. Rank 4 follows the original smallest-equivalent-eligible-rank rule. Keep
the registered B3/H3 reference and all 256 patches: no uniquely justified layer
change or eligible non-global spatial support has been established. The conditional
layer/position 2x2 cannot be populated without inventing an eligible spatial choice.
Reach-Wall has only rank 4; removing that single component is the already-measured
native arm. Push-T has no eligible primary intervention and retains native.

Reach compares these ten fixed arms on all 33 previously measured rows:

1. Native.
2. True zero dose.
3. Equal-budget coupling only.
4. Rank 4 only.
5. Equal-budget coupling plus rank 4.
6. Equal-budget coupling plus rank 1.
7. Coupling's equal-budget matched-random control.
8. Rank 4's matched-random control.
9. Both matched-random components together.
10. Matched-random coupling plus matched-random rank 1.

Composition adds unchanged fitted components at their distinct registered hooks.
Rank response probes and targets use the unedited native shadow, never a rollout
already altered by coupling. Keep the full source rank-probe/common-degeneracy
registry, fixed response radius and doses. No fitting, dose search or new basis.
Removing a component also removes its fixed energy budget; do not interpret those
drop-one differences as equal-total-energy mechanism tests. Rank-1/rank-4
comparisons and corresponding random controls retain matched component energy.

The interval family is all 16 registered contrasts times both H6 embedding-MSE
endpoints, with 10,000 paired-lineage bootstrap draws, seed 2026090704. Use the
original precision-specific 1% fit-only native-error minimum/equivalence margin.
BF16 is primary, FP32 sensitivity; do not choose between precisions. Completion
of this stage does not itself freeze a final winner or authorize confirmation.

## Exposure and execution guards

The stage verifies the prior full-pool analysis receipts for every sweep and both
precisions, the original 29-row cohort, its authorized four-row extension, and all
source fit hashes. A distinct previously-exposed-combination cohort binds exactly
33 Reach rows. Historical protection labels remain; zero new untouched rows open.
The old protected-access gate and one-factor authorization are not broadened.

Protocol frozen before combined outcomes:
`/workspace/jepa-runtime/combined-reach-author-20260907/freeze-v1`, SHA256
`81157267125e2dda20f2d43ee0f71d4f3fa3bd4bdd47dc1863b32d46e8a491f3`.

Fit-only check v1 failed because zero-valued rows through mixed hook scopes changed
FP32 striding. It is preserved. The corrected dispatcher executes actual no-op
rows on the uninstrumented model and groups active arms by hook scope. It checks
singleton fields against the unchanged source compilers before forecasting.
No failed forecast is overwritten after a comparison and no tolerance is relaxed.
Fit-only v2 passes both precisions, all ten arms and four fitting prefixes, with
native/core fidelity, singleton parity, unchanged parameters and delivered energy.
Receipt SHA256: `4c5d2b532bd423c1271c3b134e3e31fff75607a615e511efa90b55074b5c98a8`.

Eight disjoint trajectory shards (four per precision) launched at approximately
11:52 EDT on US instance 50125440. Source snapshot is `code-v20`; each GPU gets one
independent shard, no collectives or trajectory padding. The coordinator automatically
verifies coverage and analyzes both precisions after all shards finish. New output
directories preserve failed checks and prior evidence. Runtime caps are execution
safety limits, never permission to call incomplete coverage a completed study.
