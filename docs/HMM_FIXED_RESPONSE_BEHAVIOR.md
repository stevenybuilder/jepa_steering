# HMM routing around the refined fixed-response edit

## September8 priority scheduling activation

At11:54UTC the first three reference arms were almost complete, but the original
CPU scheduler waited for all five arms, including384 coupling/control episodes
that HMM does not require as references. A prospective scheduler-only handoff is
being prepared: pause only the original queue parent, allow its current complete
RNG-stream child to finish, verify native/refined/random-refined shard receipts,
then run the same v3 engineering and four routed arms on that device. Resume the
remaining original coupling controls afterward, including after a safe HMM failure.
No arm is dropped; source/fit/freeze, samples, streams, device pairing and analysis
remain unchanged. At12:17UTC all11receiving lifecycle tests and all576 required
reference episodes/traces verified; the priority scheduler was activated. Supervisors
49204–49211 pause only original parents5202–5209 while their current coupling-only
children finish intact. Coordinator49212 waits for all32 new routed shards. The
old nine CPU supervisors were administratively cancelled with explicit preserved
receipts; their cancellation markers are not scientific failures. HMM now precedes
the final192 coupling-control episodes; the already-started coupling-only streams
are not discarded or interrupted. No HMM GPU engineering or efficacy is claimed yet.

Plan SHA256: `003e4365ffe8995ce39a8bc4890eb092cc9bb897c04b5358b5841228e88dd610`.
Control root: `/workspace/jepa-runtime/routing-priority-20260908-v3`. Failed CPU
preparations v1/v2 are preserved. The original five-arm analysis remains required
and its coordinator is unchanged; the HMM analysis does not wait for that analysis.

## September8 same-pass execution amendment, before routed outcomes

The fitted scientific comparison below is unchanged. A separately named v3 source
and execution freeze replaces two runtime passes with one only after full receiving
engineering proves exact forecast, gate, posterior and delivered-field equivalence
to the preserved v2 two-pass implementation **on every engineering planner call**.
The reason is causal: no edit occurs before H3, so the same-pass H1/H2 features are
the same native features as the separate shadow. No H3–H6 routing inputs are added.
All nine complete engineering episodes and both1/300candidate paths remain required.
Scientific runtime performs one pass with no reference calls; the extra comparison
work is engineering only. No HMM/dose/normalizer/seed/sample/contrast is changed.
The v2 sources, fits, freeze and unstarted queue receipts remain historical evidence.
Failure of exact receiving equivalence blocks v3; it does not authorize tolerance
relaxation, new data selection or silently calling an approximate method equivalent.

The description below records the original two-pass reference design. Its shadow
cost applies to v2 and v3's engineering reference, not v3's scientific runtime.

September8 execution amendment, before any routed behavioral outcome. The user's
latest direction explicitly requests paired HMM behavioral ablations alongside the
refined non-routed panel. This admits the already named constant/memoryless/HMM
alternatives to behavioral development; it does not claim the old conditional
history-value eligibility requirement has passed. No forecast-significance veto,
HMM architecture search, new dose search, or confirmation access is introduced.

The underlying operator is the approved Design1 fixed map, not the stopped online
response solver. Fit the inspected archived two-state/PCA3/diagonal-Gaussian/20EM
method on the already captured native128 fit families × four prefixes per task.
H1–H6 may train emissions; each H3 decision uses only a separate native H1/H2
shadow prefix. Reset the filter for every candidate. Never smooth using H3–H6,
share beliefs across CEM iterations, or use actual future simulator outcomes.

The raw gate is0.5 in the higher TRAIN step-drift regime and1 in the other.
Divide each comparator by its own frozen TRAIN-prefix RMS, so constant equals1
and all have unit mean squared gate on the fitting population. Do not normalize
across live CEM candidates: that would let one candidate change another's edit.
This is equal FIT-expected intervention energy, not a claim that energy remains
identical on unfamiliar planner actions. Record requested and realized energy,
gate distributions and runtime; pair each gate with the corresponding learned-map
and matched-random-map treatment. Gate differences are not physical regimes.

Fit-only diagnostics are preserved: four family-disjoint folds compare H3 internal
feature predictive log density given H1/H2 against H2 alone, with the same fitted
PCA/emissions. Both task means were negative; therefore this diagnostic does NOT
establish incremental predictive history value. Normalized gate RMS separation is
nonzero, which demonstrates distinct treatments, not a beneficial intervention.
These results will not trigger refitting, different states, or changed thresholds.

## Frozen paired behavioral panel

Two MetaWorld tasks, seven arms: native; constant; memoryless; HMM; and a
matched-random control for each gate. Keep96 TOTAL/task/condition, canonical
development base2026090721, the existing corrected goal bank,100 elementary
steps, native CEM/context/actions/simulator and FP32/noTF32. No protected base1.

The current five-arm panel already measures native, the refined fixed edit and
its matched random. These can supply native/constant/random-constant endpoints
only after full source/fit/scenario hashes and full-episode constant equivalence
verify on the same receiving GPU. The raw records retain their original labels,
paths and measured runtime. This saves576 duplicate episodes across both tasks;
four newly routed arms require768 new episodes. A constant reference inherited
from the one-pass run cannot be presented as a two-pass HMM runtime measurement.
All actual new HMM/gated work includes the native-shadow cost.

Before scientific launches, validate independent static-field equivalence at
both300 candidates and the single CEM mean, zero-dose/short-horizon identity,
exact native-repeat/source/device checks, causal prefix identity and complete
fitting-scenario CEM episodes for every gate/control. Bind all checks to this fit,
source and worker. Existing scientific runs stay unchanged while these are prepared.

Analyze only complete two-task/seven-arm panels. Eight fixed contrasts per task:
HMM−memoryless, HMM−constant, each of the three gates−native, and each gate−its
matched random. The16 comparisons form a separately labelled family. Report
episode-weighted success, paired whole-scenario cluster bootstrap20,000draws,
seed2026090803 and Bonferroni simultaneous95% intervals, plus all task regressions,
paired discordance and runtime/energy diagnostics. No partial-panel selection.
Five percentage points is the existing study's practical-gain reference, not an
author quotation or a guarantee of power. No automatic confirmation reveal follows.

This tests whether history changes behavior; it does not assume HMM must improve
planning. Three trained-seed/checkpoint histories and fresh confirmation remain
separate required evidence. This initial400-feature/six-block/H6 implementation
must not be silently applied to the larger DROID/H3 architecture.
