# Refined Reach combination: paired behavioral extension

**Deferred draft — September8 user-approved core-only priority.** Not a frozen
registry or a launch-ready implementation. Do not deploy its draft module or
queue new combined behavioral episodes before review of the completed core panel.

September8,2026. This is the already requested combination/drop-one comparison,
not an additional method search. Its component banks, dose and B3/H3 operator
remain those in [the engineering contract](FIXED_COMBINED_ENGINEERING.md).
The running standalone/HMM panels and their freezes/results remain unchanged.

## Sample and compute scope

One task initially: Reach. Keep the original96 canonical development episodes
and eight persistent logical RNG streams, base seed2026090721, fixed released
checkpoint and the full author CEM/simulator settings. No protected/base-1
episode or fresh-confirmation claim is introduced. This one-task extension does
not complete the requested six-task study or training-seed/checkpoint histories.

Reuse the existing native, fixed_rank4, matched_random_fixed_rank4, coupling_only
and matched_random_coupling records after full raw-trace/receipt validation.
Run only combined_fixed_rank4 and matched_random_combined_fixed_rank4 as new
conditions:192 new scientific episodes, plus separately excluded receiving
engineering. Existing480 reference episodes retain their original labels,
timings and source hashes; they are not newly generated observations.

Each new stream must run on the SAME physical GPU as its original native
reference, using the same goal bank, initial states and logical RNG stream.
An engineering pass on Texas does not authorize cross-device reuse of LA
outcomes. Missing original controls are completed by their existing queues;
they are not silently replaced, dropped or required to finish before preparing
the combination. All seven conditions must be complete before comparative
analysis/selection. No analysis of partial outcomes changes doses or candidates.

## Frozen comparisons and interpretation

Eight primary contrasts within this named Reach extension:

1. Refined standalone versus native.
2. Refined standalone versus its matched random.
3. Coupling alone versus native.
4. Coupling alone versus its matched random.
5. Refined combination versus native.
6. Refined combination versus its matched random.
7. Refined combination versus standalone (remove coupling).
8. Refined combination versus coupling alone (remove the refined map).

The component doses are preserved, not renormalized after removal. Thus the
drop-one contrasts measure the value of adding that component at its specified
dose; they are not equal-total-energy mechanism-isolation tests.

Primary endpoint: official full-episode task success. Report paired percentage-
point differences with whole-scenario cluster bootstrap,20000draws, seed2026090722,
Bonferroni simultaneous95% intervals across these eight contrasts. Report exact
paired discordance tests only for cluster-homogeneous binary outcomes, with
Holm correction over all eight. Repeated initial/goal scenarios remain one
resampling cluster. Retain failures, null results, runtime and final-distance
regressions. The previous5pp useful-gain rule and positive native/random lower
bounds govern candidates for a LATER confirmation freeze; this report does not
grant confirmation access or establish three-trained-seed generalization.

## Required receiving evidence

Complete same-device combined numerical equivalence, then the four excluded
full-planner engineering episodes; bind all model/input/source hashes and raw
action/call records. Verify native repeat, paired stimuli,100 elementary steps,
300 candidates,15 CEM iterations per replan and the original candidate/mean
forecast schedule. Combined calls must use exactly one backend forecast, one
four-block prefix only for H6, no online response probes and no full shadow.

The new source/fits/reference/goal/config registry must be frozen before new
behavioral outcomes. Workload launch remains fail-closed on incomplete engineering,
changed source, incomplete original native streams, different device UUID,
changed canonical stimuli or duplicate/missing episode identities. Passing local
tests or publishing this document alone does not establish execution/completion.
