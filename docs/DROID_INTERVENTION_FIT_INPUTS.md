# Separate DROID intervention-fit inputs

## September8 availability amendment, before any observations/model outcomes

The first128-object proposal remains frozen and retained with its failure.122
metadata sets resolved; four complete published directories demonstrably have no
native MP4, while two other requests did not leave a directory receipt. Do not
describe all six as proven missing videos. The upstream native loader silently
resamples on load failure; this study instead makes input availability explicit.

The additive `droid_fit_availability` protocol preserves the exact original SHA
priority order/seed and requires128 camera-available recordings among at most160
priorities. The sole permitted exclusion is absence of the exact metadata-selected
left MP4 object in a complete public directory listing. Network/identity/ambiguous
metadata/decode errors stop preparation, not trigger replacement. All attempts and
exclusions are retained. Freeze this rule before querying additional priorities;
freeze the resulting object manifest before downloading any raw states or videos.
No success/lab/quality/model-result filter and no evaluation data are used. This is
an explicitly amended intervention-fit input population, not an exact author8k
training-population claim. Missing MP4s are never replaced by128px HDF5 images.
Subsequent loader parity, lineage and evaluation-disjointness gates remain required.

v2 stopped at priority75 because two metadata aliases were present, not because of
a transport error. Direct inspection found identical input fields; only collector
UUID/user_id differ. v3 permits at most four aliases only when **every other JSON
field is identical**. Preserve all aliases/hashes and deterministically use lexical
first for the native loader. This is input-equivalence resolution, not an added
exclusion or camera change. Any disagreement still stops preparation. Preserve v2
source/freeze/failure; no raw states, video or model outcomes were read in v1/v2.

v3 similarly stopped at priority104. Its two aliases differ only in UUID and the
capitalization of lab`tri`/`TRI`; the camera and all other input fields agree.
v4 case-folds only the lab identity field for equivalence, retaining its raw bytes
and aliases. Different lab names or any other input difference still fail. Both
historical failures remain. Neither adjustment excludes a recording or changes
the specified native stimuli, selection order, sample budget or analysis.

## Original proposal (preserved)

### Native clip eligibility extension (before fitting)

Availabilityv4 completed128camera-present recordings and all386objects/1.35GB
were downloaded and checksummed. The subsequent actual native-loader audit found
priorities27 and59 too short:23 and7video frames at60fps. Native4fps/fpc4 uses
stride15 and requires`randint(60,vlen)`, so neither can supply a legal clip.
No horizon reduction,padding or different-fps workaround is permitted.

The separate `droid_fit_eligible` freeze extends the same original priority order
to128 inputs that satisfy the **native** existence and clip-length predicates,
bounded by160 priorities. Its only additional exclusion is
`video_frames <= 4*ceil(actual_video_fps/4)`. Network,decode,identity,nonfinite-state,
or alignment failures are not reasons to substitute rows. No model outcomes have
been read. Existing source objects are rechecked and hard-linked, not downloaded
again; short videos and all failed proposals remain intact. Selected128rawobjects
must still pass the unchanged complete native-loader/lineage audit before fitting.
This is an explicit pre-fit data-eligibility correction, not a paper sample-size
claim or a change to the64episode behavioral evaluation.

The current DROID baseline uses the authors'15 released executable Franka
recordings and64 paired planning episodes, as documented in
[the DROID contract](DROID_METHOD_ALIGNMENT.md). Those recordings are not permitted
intervention fitting data. The requested three-seed base-model training histories
still require reconciliation of the authors'8,000-trajectory training population.

That missing base-training manifest does not prevent preparing a distinct,
prospectively fixed intervention-fitting pool. Our study already uses128 fitting
recordings/families per task. For DROID, select128 source recordings from the
completed public raw-DROID catalogue by ascending SHA256 of
`2026090804:<full source object name>`. Include success and failure folders and all
labs without an outcome filter. This is our intervention-fit budget, NOT a claim
that128 reproduces the paper's8,000 training trajectories or that the authors used
our selection. Actual independent-family count must be audited, not assumed128.

Freeze exact source object names, generations, sizes and MD5s before downloading
or reading their observations. First resolve only the source directory manifest
and its unambiguous metadata JSON. Then stage the same `trajectory.h5`, metadata,
and `left_mp4_path` camera used by the native training config, retaining generation
and byte hashes. Do not substitute Franka evaluation recordings, choose a different
camera, silently replace malformed recordings, or download unrelated stereo views.

This phase is input preparation, not an intervention fit, base-model training or
behavioral result. Before fitting, verify native loader/frame/action/RNG parity,
length and finite-state alignment, duplicate/recording lineage, and disjointness
from all evaluation-recording fingerprints. A failure leaves the proposed source
pool unresolved; it does not authorize an outcome-driven replacement recording.
The existing12-block/1024-feature/DINOv3/H3 mapping and finite candidate/control
protocol must be implemented separately; the MetaWorld HMM adapter is not reused.
