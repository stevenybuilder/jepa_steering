# Artifact map

Active experiment artifacts belong under:

`public_panel/panel-p-frankenstein-dino-reach-v1/`

The immutable manifests are tracked separately under `docs/manifests/panel-p-frankenstein-dino-reach-v1/` and are the identity source of truth.

Other directories—especially `cgs_pilot/`, driving, RoboCasa, contact, safety, and older `performance_first_v1` outputs—are preserved historical/development evidence. They do not authorize a current intervention or determine the active checkpoint/task.

Never overwrite an existing run directory or protected result. New runs require a new frozen namespace or an append-only record explicitly permitted by the canonical plan.

