# DROID native-horizon coupling addition

This is the existing vision/action factorial applied to the approved sixth task,
not a MetaWorld bank transfer or HMM no-op. No outcomes select its design.
Keep the released DROID12-block/1024-feature/no-proprio/H3 architecture and native
64episode/8persistent-stream15x300CEM protocol. Endpoint remains the authors'
recorded net-XYZ action error and checkpoint-level clipped score, not robot success.

The initial finite registry is native,visual-only,action-condition-only,joint,
equal-standardized-energy joint,permuted visual,permuted joint,matched random,
equal-standardized-energy matched random. Zero dose is a full engineering identity
check. All nine scientific arms must finish on paired inputs; no offline admission
veto or partial-result selection. Each condition has64episodes,total576.

Fit only on the separate128 native-eligible raw-DROID recordings after the full
loader/identity audit. Retain four native-RNG clips/recording,512total, the authors'
raw left camera,4fps/fpc4 and native training transforms. The fit sees no Franka
evaluation recording. No three-training-seed/author8k claim follows from this fit.

Use imagined H3, native predictor visual input and zero-indexed condition blockB6.
This maps the six-block category's B3 to the same halfway depth of12blocks,
without a layer search. Forecast nativeH3 from one observed first frame with the
three recorded action deltas and the published two-position wrapper context.
Collect native visual/condition captures; fit the unchanged12-iteration rank-one
cross-covariance power iteration, joint sign rule and robust-score scaling. Dose
is0.1fit robust sigma/component. The factorial joint keeps both doses; equal-budget
joint scales each by1/sqrt(2). Retain original random/permutation seeds and controls.
This is explicitly a DROID-specific category mapping, not a claim the authors
performed steering or that their paper prescribes our hook site.

The runtime adds fixed precomputed GPU-resident fields in the same native pass.
No online probes, native shadows, changed candidate actions or skipped CEM work.
Report requested and realized energy. Native/zero and short horizons remain native.
Receiving full-CEM checks must independently compile the same fields at1and300
candidates, test native repeats/zero exactly and retain every registered arm's
full engineering episode before scientific execution.

Before behavioral launch, freeze exact source/fit/input/device bindings, paired
64episode streams, all contrasts and analysis. Use the existing DROID1-score-point
practical reference and a recording-cluster paired simultaneous95% analysis;
retain source episodes/RNG, recorded/planned actions and runtime. The existing64
native records may be reused only after strict source/device/stimulus equivalence;
otherwise run a new complete native reference, not silentlymix device runtimes.
Other DROID interventions, seed histories and fresh-recording confirmation remain
separate requirements. `droid_coupling.py`, `droid_coupling_fit.py` and
`droid_coupling_behavior.py` implement fitting, engineering, execution and analysis.
At 11:08 UTC September 8, the v2 fit and all eleven full-planner engineering
episodes are complete. The native/repeat/zero episodes exactly reproduced the
original baseline; all field-reference checks passed. Eight visual-only scientific
episodes are complete, with the next arm underway. This is an incomplete panel,
not evidence of efficacy. The v1 wrong-interpreter failure is preserved separately.

Fit bank SHA256: `2c4341f1b49c8deb13b858cfd98854fdb5b10c5158692a7704993be025c62d23`.
v2 engineering report: `27946b1da5163c9467150bc32ff689973890d01fef4545da55aeb7d24d06a118`.

For a prospective, outcome-independent four-GPU move, v3 passed eight receiving
CPU tests and froze source `249a8cdb9326cd79f3b9180a830e409cdd0ffa4f2f3be2ac393c32e30ab81a54`,
protocol `369d632792b6b482f5c63bbcd599dd7576cf949001ac005baa1cb5f600681862`.
The fit, nine arms, planner, seeds and 64-total sample count are unchanged. GPU g
owns persistent streams g and g+4, with 16 episodes per arm per GPU. Each device
first requires native/repeat/actual-zero and all-arm engineering, then supplies
its own native streams before paired candidate runs. Cross-device native outcomes
need not be bitwise identical; stimulus identity and same-device repeat/zero
identity remain mandatory. Do not pool the partial v2 panel into this comparison.
At 11:54 UTC, all four Texas receiving devices completed all eleven full-CEM
engineering episodes (900–919 seconds per device). Same-device repeat/zero,
independent fields at1/300 candidates and unchanged parameters passed. Original
native engineering also matched exactly, but the new paired-native policy remains
unchanged. Four scientific queues automatically started native streams0–3, each
followed by its fixed candidates and second stream. No full-panel efficacy result
exists yet. The preserved v2 panel contains16 candidate episodes and is not pooled.
The new source/engineering archive passed full private-Drive member readback:
`live-20260908T115600Z/droid-parallel`, SHA256
`7a2978278da32a9fd8b03298be30ba5e89ecc725028d8893e564add7bd869b24`.
PointMaze234's exact epoch-3 state and earlier history are preserved for required
resumption after the priority DROID work; no seed or training epoch is discarded.
