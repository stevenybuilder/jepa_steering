# Resume record, 2026-09-07

Owner: `rep_geometry_transcoder/root`, user instruction: “keep going”.
Governing protocol: `docs/EXPERIMENT_PLAN.md` and `configs/study.json`.

## Coupling equal-budget completion

The preserved completed runs used eight arms. The current plan requires ten,
including equal-standardized-energy joint and its matched random control. Reuse
the original verified fit tensors and component doses without refitting. Freeze a
new protocol hash, rerun all ten arms on the same reviewed development population,
and retain both versions. This is a prospectively recorded development extension,
not independent confirmation or retrospective completion of the original run.

Hypothesis: the registered coupling effect persists when the joint components each
receive `1/sqrt(2)` of their original standardized dose. Primary contrasts are in
the frozen generated protocol; existing eight arm definitions remain identical.
The existing descriptive lineage analysis does not select a final intervention.

Resources: resume only project-owned Vast `50125440`, eight RTX 5090 GPUs,
approximately $2.954/hour running. Other three instances remain stopped. Use
one process per GPU and unique roots under
`/workspace/jepa-runtime/resume-20260907/coupling/`. Start with a bounded runtime
check, then execute all three tasks. Each job has a runtime cap; preserve and
verify results locally before stopping compute.

Efficiency: reuse already fitted directions, set CPU numerical libraries to one
thread per process, and reduce delivered edit norms on the GPU. These changes
must pass identity checks. No mixed precision or trajectory exclusions are added.

Status: ten-arm development complete on all three tasks and copied locally under
`artifacts/offline_study/resume-20260907/remote/coupling/`. Push-T took 318.052 seconds
for 3,636 rollouts, 36 families, 14,544 windows and 145,440 arm evaluations on six
GPUs. The preceding eight-arm run took 1,781.668 seconds on eight GPUs. This is a
5.60x shorter observed wall time after the combined CPU-thread/norm changes,
despite evaluating 25% more arms; it is not an isolated attribution to either fix.

Retained failed launch records: `runtime-check` used the default Python instead
of `/workspace/jepa-python/bin/python`; the first Reach/Reach-Wall launches omitted
`LD_LIBRARY_PATH=/opt/conda/lib` required by TorchCodec. Successful recovery uses
the same frozen fits and protocol, under `development-recovery`. No outcome-based
changes or exclusions occurred.

## Completed geometry sweep

Action-response geometry implementation is next. The archived four-anchor
estimator will be explicitly ported and tested. All donor estimates omit t=0;
only the t=0 recorded-action recipient has a recorded physical target. Raw
reconstruction and forecast fidelity must be reported separately from the
common-energy interventions. No confirmation data will be opened.

The reference is now fixed in `docs/ACTION_GEOMETRY.md`; 53 tests pass locally and
on the remote Python runtime. First fitting launch failed before activation capture:
constructing the model inside `torch.inference_mode` disabled tensor version
counters required by the frozen-weight audit. Corrected to `torch.no_grad` during
construction, retaining inference mode for donor rollouts. Original failures remain
in `geometry/`; retry uses `geometry-v2/` and a separate `geometry-code-v2/` snapshot.

All three corrected geometry fits and full development comparisons completed.
Fitting used 128 families and 512 windows per task. Development populations in
both completed sweeps are Reach 28 rollouts/28 lineages/112 windows, Reach-Wall
25/24/100, and Push-T 3,636/36/14,544. Geometry Push-T took 477.810 seconds on six
GPUs. Complete results are local at
`artifacts/offline_study/resume-20260907/remote/geometry-v2/`.

No geometry arm met the frozen useful-improvement criterion against native and
random controls; all three tasks retain native. Push-T's raw cubic omitted-center
activation reconstruction MSE was 7.0173e-8 versus linear 6.9839e-5 (about 995x
lower). This is reconstruction of the model, not physical prediction improvement.
The equal-energy cubic-versus-native H6 proprioception-error difference was
-6.2655e-9, with simultaneous 95% interval [-1.5531e-8, 2.9996e-9]; no useful gain.

Local closure verified 132 referenced artifact hashes across both successful
suites. Geometry source hash is
`fe3ff9aa58cb13d97046020a3c6911cbe685d8da8bd3941348fdc5b9d5eae1c9`.
Coupling's source hash includes AppleDouble metadata from its initial Mac tar;
the exact source is preserved in `coupling-source-exact.tar.gz`, whose member
bytes reproduce `e9c59b66154f0c0b9f44f0d1ceddc39ae569c4e653b3c0082891d5839eeef230`.
No result or source discrepancy was found. All four instances were verified
stopped after this closure; storage remained approximately $0.264/hour.

## Newly authorized remaining sweeps

The user subsequently requested all four current instances for rank/layer/spatial
tests and reiterated fixed design, not adaptive autoresearch. The exact shared
PCA3+OLS H6 visual-residual correction reference is `docs/SUPPORT_SWEEPS.md`.
Source tests: 58 pass locally and on the prepared GPU worker. A pre-development
review corrected the scattered-position selection to an explicit spaced-grid
family; first fitting outputs are retained, native fitting captures are reused
by hash, and corrected fitting uses a new `fits-v3` directory.

Resources: `50125440` plus `50135088/89/90`, 32 advertised RTX 5090 GPUs,
approximately $12.64/hour aggregate running. China workers initially lacked usable
Python/data; official public inputs and runtime are being staged directly from the
prepared US worker through authenticated SSH. No private key is copied remotely.
Root: `/workspace/jepa-runtime/support-sweeps-20260907/`. Completion of these jobs
will be reported only after their receipts and copies verify. Routing remains
conditional, and protected/closed-loop evaluation has not run.
